/**
 * Logo file in `public/` — served from site root.
 */
export const SITE_LOGO_SRC = '/12gig-logo.png';

/** Set to `false` when the image is a full lockup (logo + wordmark). */
export const SITE_LOGO_SHOW_WORDMARK = false;
